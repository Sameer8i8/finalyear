---
- name: Check hostname on RHEL systems
  hosts: localhost
  become: true
  tasks:
    - name: Gather hostname information
      ansible.builtin.command:
        cmd: hostnamectl
      register: hostname_info

    - name: Display the current hostname
      ansible.builtin.debug:
        msg: "The current hostname is: {{ hostname_info.stdout }}"
